name: Build & Test

on: [push]

permissions:
  contents: read

jobs:
  build:
    name: Build Static Site
    uses: gsa/data.gov/.github/workflows/static-site-build-template.yml@main
    with:
      ruby-version: "3.2.11"
